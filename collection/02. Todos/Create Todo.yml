info:
  name: Create Todo
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/todos"
  body:
    type: json
    data: |-
      {
        "title": "WITH USER",
        "description": "description two",
        "priority": 2,
        "complete": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
