info:
  name: Delete A Todo
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/todos/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
