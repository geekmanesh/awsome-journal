info:
  name: Delete Todo
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{base_url}}/admin/todos/3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
