info:
  name: Update Partial
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{base_url}}/users/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
