info:
  name: Change Password
  type: http
  seq: 2

http:
  method: PUT
  url: "{{base_url}}/users/"
  body:
    type: json
    data: |-
      {
        "password": "sikim",
        "new_password": "sikkkimmm"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
