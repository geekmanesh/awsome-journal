info:
  name: Update Todo
  type: http
  seq: 4

http:
  method: PUT
  url: "{{base_url}}/todos/1"
  body:
    type: json
    data: |-
      {
        "title": "1234",
        "description": "HELLO ITS ME",
        "priority": 2,
        "complete": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
